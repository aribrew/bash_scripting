#!/bin/bash

LOCAL_HELPERS="$HOME/.local/bin/bash_helpers"
SYSTEM_HELPERS="/opt/bin/bash_helpers"


if [[ -f "$LOCAL_HELPERS" ]];
then
    source "$LOCAL_HELPERS"

elif [[ -f "$SYSTEM_HELPERS" ]];
then
    source "$SYSTEM_HELPERS"
fi
